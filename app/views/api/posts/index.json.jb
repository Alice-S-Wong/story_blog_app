@posts.map do |post|
  {
    id: post.id,
    chapter_number: post.chapter_number,
    title: post.title,
    body: post.body,
    release_date: post.release_date,
    friendly_release_date: post.friendly_release_date,
    story_id: post.story_id,
    story_title: post.story.title,
  }
end